---
layout: single
title: "D3 : [ 문제 선정 & 해결 ]"
---

[ 문제상황 ]

```python
IT 기업 호스팅.hd에서는 나만의 도메인을 만들도록 최적 가격 프로모션을 진행하고 있습니
다. 다음의 도메인 생성 규칙을 보고 인터넷 주소를 만드는 프로그램을 작성해 보시오.
```

[ 소스코드 ]


```python

#Bellflower

constit_allowed_name = [ "co", "or", "ne", "re", "pe" ]
country_allowed_name = [ "kr", "cn", "sg", "in", "io" ]
type_of_web = [ "http", "https" ]
Running = True

while Running:
  name = "bellflower"
  constit = str(input("다음중 선택하실 기관명을 정해주세요. " + str(constit_allowed_name) + " :")).lower()
  country = str(input("다음중 선택하실 국가명을 정해주세요. " + str(country_allowed_name) + " :")).lower()
  http_or_https = str(input("다음중 선택하세요. " + str(type_of_web) + " :")).lower()

  if constit in constit_allowed_name:
    allow_constit = True
  if country in country_allowed_name:
    allow_country = True
  if http_or_https in type_of_web:
    allow_https_or_https = True


  if allow_country == True or allow_constit == True or http_or_https == True:
        print(str(http_or_https) + "://www." + str(name) + "." + str(constit) + "." + str(country))
  else:
    print("등록되지않은 타입을 선택하셨습니다. 다시 선택해주십시오.")
 
 ```
  
  
  [ 결과 ]

```python
다음중 선택하실 기관명을 정해주세요. ['co', 'or', 'ne', 're', 'pe'] :co
다음중 선택하실 국가명을 정해주세요. ['kr', 'cn', 'sg', 'in', 'io'] :kr
다음중 선택하세요. ['http', 'https'] :http
http://www.bellflower.co.kr
```
